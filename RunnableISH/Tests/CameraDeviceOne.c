
void startenergy()
{
//we have to get a model that is suited for heat transfer equations.
  //so lets test the model twice.
  einsum(gotmodel);
  einsum(gormodel);
  //we have to cancel ciphers now. lets donit rwoxe acain.

  //now lets see what cpu register it can use

    //lets turn the model to diagonal left. this is best.

}

void createCaleraDevixd()
{
kDevixeErrorMessage->sendto();

  //nowadays decixes have heavy consuming energy.
kDevixeProxess->fillWith(modelone,model2,model3);
  
}
