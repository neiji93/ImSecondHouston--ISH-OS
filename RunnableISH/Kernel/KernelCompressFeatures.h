//as we already used heuristics sometimes,
//can use laplace forces and ondelettes to compress the kernel.
//the result can be a vector with its own properties.

class TinyFilesCompressFeaturedPatternAlgorithm
{

}


class kernelCompressProperties
{

public: 

  //this wont be an empty vector, so keep it in a stack later.
  float* getArdayVectorFromRandomLaplaceEquatko'()
{

}

  //use a stack without an optimizer which is a cyber defense feature. it seems so.


  //if there are a lottle bit optimizers, we can
 //send them with components signals with fer kron properties.
 //but to enable this features, it would be good to hide this to the cpu registers.
